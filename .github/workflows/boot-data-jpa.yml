name: boot-data-jpa

on:
  push:
    paths:
      - "boot-data-jpa/**"
    branches:
      - master
  pull_request:
    paths:
      - "boot-data-jpa/**"
    types:
      - opened
      - synchronize
      - reopened

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: "temurin"
          java-version: 21
          cache: "maven"

      - name: Build with Maven
        run: mvn -B -q clean package --file boot-data-jpa/pom.xml
        

