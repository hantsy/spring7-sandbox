name: boot-kafka-share-consumer-prog-container

on:
  push:
    paths:
      - "boot-kafka-share-consumer-prog-container/**"
    branches:
      - master
  pull_request:
    paths:
      - "boot-kafka-share-consumer-prog-container/**"
    types:
      - opened
      - synchronize
      - reopened

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          distribution: "temurin"
          java-version: 25
          cache: "maven"

      - name: Build with Maven
        run: mvn -B -q clean package --file boot-kafka-share-consumer-prog-container/pom.xml
        

