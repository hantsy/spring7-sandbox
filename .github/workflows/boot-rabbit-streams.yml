name: boot-rabbit-streams

on:
  push:
    paths:
      - "boot-rabbit-streams/**"
    branches:
      - master
  pull_request:
    paths:
      - "boot-rabbit-streams/**"
    types:
      - opened
      - synchronize
      - reopened

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          distribution: "temurin"
          java-version: 25
          cache: "maven"

      - name: Build with Maven
        run: mvn -B -q clean package --file boot-rabbit-streams/pom.xml
        

