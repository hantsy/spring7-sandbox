name: jms-messaging

on:
  push:
    paths:
      - "jms-messaging/**"
    branches:
      - master
  pull_request:
    paths:
      - "jms-messaging/**"
    types:
      - opened
      - synchronize
      - reopened

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          distribution: "temurin"
          java-version: 25
          cache: "maven"

      - name: Build with Maven
        run: mvn -B -q clean package --file jms-messaging/pom.xml
        

