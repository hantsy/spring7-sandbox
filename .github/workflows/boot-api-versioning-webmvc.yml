name: boot-api-versioning-webmvc

on:
  push:
    paths:
      - "boot-api-versioning-webmvc/**"
    branches:
      - master
  pull_request:
    paths:
      - "boot-api-versioning-webmvc/**"
    types:
      - opened
      - synchronize
      - reopened

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          distribution: "temurin"
          java-version: 25
          cache: "maven"

      - name: Build with Maven
        run: mvn -B -q clean package --file boot-api-versioning-webmvc/pom.xml
        

