name: boot-http-service-client-webclient

on:
  push:
    paths:
      - "boot-http-service-client-webclient/**"
    branches:
      - master
  pull_request:
    paths:
      - "boot-http-service-client-webclient/**"
    types:
      - opened
      - synchronize
      - reopened

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          distribution: "temurin"
          java-version: 25
          cache: "maven"

      - name: Build with Maven
        run: mvn -B -q clean package --file boot-http-service-client-webclient/pom.xml
        

