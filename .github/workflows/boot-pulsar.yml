name: boot-pulsar

on:
  push:
    paths:
      - "boot-pulsar/**"
    branches:
      - master
  pull_request:
    paths:
      - "boot-pulsar/**"
    types:
      - opened
      - synchronize
      - reopened

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: "temurin"
          java-version: 21
          cache: "maven"

      - name: Build with Maven
        run: mvn -B -q clean package --file boot-pulsar/pom.xml
        

